<UserControl x:Class="Build_IT_Desktop.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:Build_IT_Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Build_IT_Desktop;component/Resources/ResourceDictionaries/ColorsResourceDictionary.xaml"/>
    </UserControl.Resources>
    <Border Grid.Row="0" 
            Background="{StaticResource GrayBrush}" 
            MouseLeftButtonDown="Special_DragWindow" >
        <Grid>
            <TextBlock Text="Building-IT" />
            <!--Style="{StaticResource HeaderTextBlock}"-->
            <Border VerticalAlignment="Top" 
                    HorizontalAlignment="Right" 
                    Margin="0,0,00,0">
                <ContentControl prism:RegionManager.RegionName="WindowControlsRegion" />
            </Border>
        </Grid>
    </Border>
</UserControl>
